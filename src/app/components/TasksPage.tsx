// "use client";

// import React from "react";
// import TasksPageEnhanced from "./TasksPageEnhanced";

// export default function TasksPage({ context }: { context?: { company: string } }) {
//   return <TasksPageEnhanced context={context} />;
// } 
